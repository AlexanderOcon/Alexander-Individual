import React from "react";
import {Form, Button, Card, Alert}from "react-bootstrap";

const FormularioLogin = ({usario,contrasena, error, setUsuario, setContrasena, IniciarSesion}) => {

    return ();

    };

export default FormularioLogin;